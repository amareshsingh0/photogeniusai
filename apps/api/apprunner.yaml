version: 1.0
runtime: python311
build:
  commands:
    build:
      - pip install -r requirements.txt
run:
  command: uvicorn app.main:app --host 0.0.0.0 --port 8080 --workers 4
  network:
    port: 8080
  env:
    - name: PORT
      value: "8080"
    - name: ENVIRONMENT
      value: "production"
