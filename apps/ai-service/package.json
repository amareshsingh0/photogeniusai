{
  "name": "@photogenius/ai-service",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "node ../../scripts/run-ai-dev.mjs",
    "start": "uvicorn app.main:app --host 0.0.0.0 --port 8000",
    "lint": "echo 'Python linting skipped in Node context - run ruff/black directly'",
    "format": "echo 'Python formatting skipped in Node context - run black directly'",
    "test": "echo 'Python tests skipped in Node context - run pytest directly'"
  }
}
