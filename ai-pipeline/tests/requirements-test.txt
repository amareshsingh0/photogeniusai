# Optional: for pytest async tests
pytest>=7.0.0
pytest-asyncio>=0.21.0

# Math/diagram renderer tests (install from ai-pipeline root: pip install -r requirements.txt)
# sympy>=1.12
# antlr4-python3-runtime>=4.11
# matplotlib>=3.7.0
# Pillow>=10.0.0
# numpy (usually already present)
